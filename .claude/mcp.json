{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser", "chromium",
        "--isolated",
        "--user-data-dir", "/tmp/playwright-torugart",
        "--ignore-https-errors"
      ]
    }
  }
}
